[project]
name = "langgraph-demo"
version = "0.1.0"
description = "A basic LangGraph demo"
requires-python = ">=3.10"
dependencies = [
    "langgraph>=0.2.0",
    "langchain-openai>=0.2.0",
    "langchain-core>=0.3.0",
]

[tool.uv]
dev-dependencies = [
    "python-dotenv>=1.0.0",
]
